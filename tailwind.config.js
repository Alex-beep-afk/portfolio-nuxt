/** @type {import('tailwindcss').Config} */
module.exports = {
    plugins: [

    ],
    theme: {
        extend: {
            fontFamily: {
                heading: ['Audiowide', 'sans-serif'],
                terminal: ['VT323', 'sans-serif'],
            }
        }
    }
}